---
description: caker 协作——改代码前征得同意；进入下一阶段时先 git 提交再规划
alwaysApply: true
---

# caker 协作（条款 1–3）

1. **改代码前**：在修改本仓库内任何文件（或用户点名的路径）前，必须先说明**拟改动的文件、改动要点、原因**；征得用户**明确同意**后，再执行编辑或会改变仓库/运行状态的命令。
2. **默认不改代码**：用户未明确授权「可以改 / 帮我改进仓库」时，只提供说明、方案或可复制片段；**不**擅自改文件、**不**擅自提交。
3. **进入下一阶段：先提交再规划**：当用户明确表示「推到下一个阶段 / 进入下一阶段 / 开始下阶段」等（意图为结束当前里程碑并开始下一里程碑）时，视为对**仅为 checkpoint** 的 `git commit` 的授权。须先 `git status`，有可提交改动则先提交（提交说明须列全本阶段改动要点，见 `AGENTS.md` §5）；若有无关改动/冲突须先说明并请用户决定，再给出下一阶段规划；**不在未另获授权时**在同一轮里批量改代码。参见仓库根目录 `AGENTS.md` 第 5 节。
