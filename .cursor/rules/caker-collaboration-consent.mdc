---
description: caker 协作——改代码前征得同意；进入下一阶段先提交再按指南分工贴全码
alwaysApply: true
---

# caker 协作（条款 1–3）

1. **改代码前**：在修改本仓库内任何文件（或用户点名的路径）前，必须先说明**拟改动的文件、改动要点、原因**；征得用户**明确同意**后，再执行编辑或会改变仓库/运行状态的命令。
2. **默认不改代码**：用户未明确授权「可以改 / 帮我改进仓库」时，只提供说明、方案或可复制片段；**不**擅自改文件、**不**擅自提交。
3. **进入下一阶段：先提交再规划并贴代码**：当用户明确表示「推到下一个阶段 / 进入下一阶段 / 开始下阶段」等（意图为结束当前里程碑并开始下一里程碑）时，视为对**仅为 checkpoint** 的 `git commit` 的授权。须先 `git status` 并视情况提交（见 `AGENTS.md` §5）；随后**必须**按 `docs/agent_skills_build_guide.md` 下一 M 的清单与 `[我写]`/`[你手敲]`/`[一起写]`，以约定格式**贴全代码**（每块标明新建/续写、**分工：我写｜你手敲｜一起写**、块后解说；指南「你手敲」亦须完整贴出）。贴码≠改仓库，改文件仍须 §1。详见 `AGENTS.md` 第 5 节。
